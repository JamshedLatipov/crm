# Миграция Contacts на универсальную систему фильтрации

Дата: 12 января 2026 г.

## ✅ Выполнено

### 1. Обновлён `contacts.component.ts`

#### Импорты
- ✅ Удалён `FiltersDialogComponent` (старый)
- ✅ Добавлен `UniversalFiltersDialogComponent` (новый)
- ✅ Добавлен `UniversalFilterService`
- ✅ Добавлены интерфейсы `FilterFieldDefinition`, `BaseFilterState`
- ✅ Добавлены enums `ContactType`, `ContactSource` для определений полей

#### Определения статических полей
Добавлено 9 статических полей:
```typescript
private staticFields: FilterFieldDefinition[] = [
  { name: 'type', label: 'Тип контакта', type: 'select', selectOptions: [...] },
  { name: 'source', label: 'Источник', type: 'select', selectOptions: [...] },
  { name: 'email', label: 'Email', type: 'email' },
  { name: 'phone', label: 'Телефон', type: 'phone' },
  { name: 'companyName', label: 'Компания', type: 'text' },
  { name: 'position', label: 'Должность', type: 'text' },
  { name: 'assignedTo', label: 'Ответственный', type: 'text' },
  { name: 'createdAt', label: 'Дата создания', type: 'date' },
  { name: 'isBlacklisted', label: 'В черном списке', type: 'boolean' },
];
```

#### Инжект сервиса
```typescript
private readonly universalFilterService = inject(UniversalFilterService);
```

#### Метод `openFiltersDialog()`
- ✅ Преобразование `CustomFieldDefinition` в `FilterFieldDefinition`
- ✅ Использование `UniversalFiltersDialogComponent` вместо старого
- ✅ Передача статических и кастомных полей
- ✅ Обработка результата с типом `BaseFilterState`

#### Методы работы с фильтрами
- ✅ `getOperatorLabel()` - теперь использует `universalFilterService.getOperatorLabel()`
- ✅ `getTotalActiveFilters()` - теперь использует `universalFilterService.countActiveFilters()`

### 2. Удалён старый компонент

```bash
rm -rf apps/front/src/app/contacts/components/filters-dialog/
```

Удалены файлы:
- ❌ `filters-dialog.component.ts` (338 строк)
- ❌ `filters-dialog.component.html` (224 строки)
- ❌ `filters-dialog.component.scss` (стили)

**Итого удалено:** ~600 строк дублирующего кода

## 📊 Результаты

### Было
```
contacts/
├── components/
│   └── filters-dialog/          # 600+ строк
│       ├── filters-dialog.component.ts
│       ├── filters-dialog.component.html
│       └── filters-dialog.component.scss
└── contacts.component.ts        # Дублированная логика
```

### Стало
```
shared/
└── dialogs/
    └── universal-filters-dialog/   # Переиспользуемый компонент
        ├── universal-filters-dialog.component.ts
        ├── universal-filters-dialog.component.html
        └── universal-filters-dialog.component.scss

contacts/
└── contacts.component.ts           # Использует универсальный компонент
```

## 🎯 Преимущества

### ✅ Меньше кода
- Удалено ~600 строк из contacts
- Единый источник правды
- Легче поддерживать

### ✅ Согласованность
- Одинаковый UX во всех модулях
- Единообразное поведение
- Общие стили

### ✅ Типобезопасность
- Использование общих интерфейсов
- TypeScript проверки
- Автокомплит в IDE

### ✅ Готовность к масштабированию
- Leads могут использовать тот же компонент
- Companies могут использовать тот же компонент
- Deals могут использовать тот же компонент

## 🔧 Технические детали

### Преобразование Custom Fields

Старый формат (`CustomFieldDefinition`):
```typescript
{
  id: string;
  name: string;
  label: string;
  fieldType: 'text' | 'number' | ...;
  selectOptions?: SelectOption[];
}
```

Новый формат (`FilterFieldDefinition`):
```typescript
{
  name: string;
  label: string;
  type: 'text' | 'number' | ...;
  selectOptions?: Array<{ label: string; value: string }>;
}
```

Преобразование в коде:
```typescript
const customFields: FilterFieldDefinition[] = this.customFieldDefinitions().map(def => ({
  name: def.name,
  label: def.label,
  type: def.fieldType as FilterFieldDefinition['type'],
  selectOptions: def.selectOptions?.map(opt => ({
    label: String(opt),
    value: String(opt),
  })),
}));
```

### Совместимость типов

`ContactsFilterState` расширяет `BaseFilterState`:
```typescript
// BaseFilterState (shared)
interface BaseFilterState {
  search?: string;
  isActive?: boolean;
  filters: UniversalFilter[];
}

// ContactsFilterState (contacts-specific)
interface ContactsFilterState {
  search?: string;
  isActive?: boolean | null;  // Может быть null
  filters: UniversalFilter[];
}
```

Приведение типов:
```typescript
// При передаче в диалог
initialState: this.filterState as BaseFilterState

// При получении результата
this.filterState = result as ContactsFilterState;
```

## ✨ Функциональность

### Поддерживаемые возможности
- ✅ Поиск по тексту
- ✅ Фильтр активности (активные/неактивные/все)
- ✅ 9 статических полей для фильтрации
- ✅ Динамические кастомные поля
- ✅ 12 операторов сравнения
- ✅ Автоопределение типа input (text/select/date)
- ✅ Select для enum и boolean полей
- ✅ Чипсы для отображения активных фильтров
- ✅ Подсчёт активных фильтров
- ✅ Удаление отдельных фильтров
- ✅ Очистка всех фильтров

### UI/UX
- ✅ Табы для статических и кастомных полей
- ✅ Кнопки добавления фильтров
- ✅ Удаление фильтров по кнопке
- ✅ Сброс всех фильтров
- ✅ Визуальная индикация типа поля
- ✅ Адаптивный дизайн

## 🧪 Тестирование

### Что нужно проверить:
1. ✅ Открытие диалога фильтров
2. ⏳ Добавление статических фильтров
3. ⏳ Добавление кастомных фильтров
4. ⏳ Изменение операторов
5. ⏳ Ввод значений
6. ⏳ Select для enum полей
7. ⏳ Применение фильтров
8. ⏳ Отображение чипсов
9. ⏳ Удаление фильтров через чипсы
10. ⏳ Очистка всех фильтров
11. ⏳ Поиск + фильтры одновременно
12. ⏳ Пагинация с фильтрами

### Проверка backend integration:
- ⏳ Запрос `POST /contacts/advanced-search`
- ⏳ Передача правильных параметров
- ⏳ Получение отфильтрованных данных
- ⏳ Корректный total count

## 📝 Следующие шаги

### Краткосрочные (сегодня)
1. ⏳ Протестировать фильтрацию на странице Contacts
2. ⏳ Проверить все операторы
3. ⏳ Проверить статические и кастомные поля
4. ⏳ Убедиться в корректной работе с backend

### Среднесрочные (1-2 дня)
5. ⏳ Мигрировать Leads на универсальную систему
6. ⏳ Мигрировать Companies на универсальную систему
7. ⏳ Мигрировать Deals на универсальную систему

### Долгосрочные (1 неделя)
8. ⏳ Написать unit тесты
9. ⏳ Написать E2E тесты
10. ⏳ Добавить документацию для разработчиков

## 🐛 Известные ограничения

Те же что и раньше:
1. **Between operator** - требует ровно 2 значения
2. **Array operators** - нет UI для множественного ввода
3. **Date fields** - нет date picker
4. **Локализация** - только русский

## 💡 Возможные улучшения

1. **Date picker** для полей с типом `date`
2. **Multi-input** для операторов `in`, `not_in`, `between`
3. **Валидация** значений перед отправкой
4. **Предпросмотр** количества результатов
5. **Сохранение** часто используемых фильтров

---

**Автор:** GitHub Copilot  
**Дата:** 12 января 2026 г.  
**Ветка:** feat/contacts-costom-fields

**Статус:** ✅ Миграция завершена, готово к тестированию
