; Minimal logging to prevent disk fill
[general]
dateformat=%F %T
appendhostname = yes
queue_log = yes

; Avoid verbose/debug logs to files.
[logfiles]
; Debug logging for development
[general]
dateformat=%F %T
appendhostname = yes
queue_log = yes

; Enable verbose and debug for messages during debugging session.
[logfiles]
messages => notice,warning,error,verbose,debug
security => security
; full => notice,warning,error,verbose(1)
