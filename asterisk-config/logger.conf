; Minimal logging to prevent disk fill
[general]
dateformat=%F %T
appendhostname = yes
queue_log = yes

; Avoid verbose/debug logs to files.
[logfiles]
messages => notice,warning,error
security => security
; full => notice,warning,error,verbose(1)  ; Uncomment temporarily for debugging
