; Minimal logging configuration to avoid CPU/IO pressure in container
[general]
dateformat=%F %T
appendhostname = yes
queue_log = yes

[logfiles]
messages => notice,warning,error
security => security

; To enable verbose/debug for troubleshooting, temporarily change the
; messages line above to include verbose,debug and reload logger.
