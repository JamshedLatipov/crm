[default]
exten => 1000,1,Answer()
exten => 1000,n,Playback(demo-congrats)
exten => 1000,n,Hangup()

; Operator extensions
exten => 2001,1,Answer()
exten => 2001,n,Playback(hello-world)
exten => 2001,n,Hangup()

exten => 2002,1,Answer()
exten => 2002,n,Playback(hello-world)
exten => 2002,n,Hangup()

exten => 2003,1,Answer()
exten => 2003,n,Playback(hello-world)
exten => 2003,n,Hangup()

exten => 2004,1,Answer()
exten => 2004,n,Playback(hello-world)
exten => 2004,n,Hangup()

; Allow operators to call each other
exten => _200X,1,NoOp(Calling extension ${EXTEN})
exten => _200X,n,Dial(PJSIP/${EXTEN},20)
exten => _200X,n,Hangup()

; Включаем запись разговоров
exten => h,1,NoOp(Hung up)
exten => h,n,GotoIf($["${CDR(recordingfile)}" = ""]?norecord)
exten => h,n,System(chmod 777 ${CDR(recordingfile)})
exten => h,n(norecord),Hangup()
