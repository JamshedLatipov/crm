# Унификация отображения переменных в шаблонах

**Дата:** 7 января 2026 г.  
**Ветка:** fix/small-bugs

## Изменение

Переменные во всех типах шаблонов (WhatsApp, Telegram, Email) теперь отображаются в боковой панели (sidebar), как это сделано в SMS шаблонах, вместо встроенной секции внутри формы.

## Мотивация

- **Единообразие UX**: Все типы шаблонов имеют одинаковый интерфейс
- **Лучшая читаемость**: Переменные всегда видны справа, не нужно прокручивать
- **Больше места**: Форма занимает основную часть экрана, боковая панель - дополнительную информацию
- **Профессиональный вид**: Sidebar дизайн более современный и структурированный

## До изменений

### Старый дизайн (inline переменные)
```html
<div class="detected-variables">
  <h4>
    <mat-icon>code</mat-icon>
    Обнаруженные переменные в шаблоне:
  </h4>
  <div class="variables-list">
    <mat-chip>{{name}}</mat-chip>
    <mat-chip>{{email}}</mat-chip>
  </div>
</div>
```

**Проблемы:**
- Занимало место внутри формы
- Нужно было прокручивать, чтобы увидеть
- Использовались Material Chips (дополнительная зависимость)
- Разный дизайн у SMS (с sidebar) и остальных

## После изменений

### Новый дизайн (sidebar layout)

```html
<div class="form-layout">
  <!-- Основная форма слева -->
  <mat-card class="form-card">
    <form>
      <!-- Поля формы -->
    </form>
  </mat-card>

  <!-- Боковая панель справа -->
  <mat-card class="sidebar-card">
    <div class="sidebar-section">
      <h3 class="sidebar-title">
        <mat-icon>code</mat-icon>
        Переменные
      </h3>
      
      @if (detectedVariables().length > 0) {
        <div class="variables-list">
          @for (variable of detectedVariables(); track variable) {
            <div class="variable-item">
              <mat-icon>bookmark</mat-icon>
              <span>{{'{{'}}{{ variable }}{{'}}'}}</span>
            </div>
          }
        </div>
      } @else {
        <div class="empty-variables">
          <mat-icon>info</mat-icon>
          <p>Переменные не обнаружены</p>
          <small>Добавьте {{'{{'}}имя{{'}}'}} в текст</small>
        </div>
      }
    </div>

    <div class="sidebar-section">
      <h3 class="sidebar-title">
        <mat-icon>tips_and_updates</mat-icon>
        Подсказки
      </h3>
      <ul class="tips-list">
        <li>Подсказка 1</li>
        <li>Подсказка 2</li>
        <li>Подсказка 3</li>
      </ul>
    </div>
  </mat-card>
</div>
```

## Структура Layout

### Desktop (ширина > 1024px)
```
┌────────────────────────────────────────────┬─────────────────────┐
│                                            │                     │
│          Основная форма                    │    Боковая панель  │
│          (form-card)                       │    (sidebar-card)   │
│                                            │                     │
│  - Название                                │  📋 Переменные      │
│  - Категория                               │    • {{name}}       │
│  - Контент                                 │    • {{email}}      │
│  - Медиа (если есть)                       │                     │
│  - Кнопка (если есть)                      │  💡 Подсказки       │
│  - Активен                                 │    • Подсказка 1    │
│                                            │    • Подсказка 2    │
│                                            │                     │
└────────────────────────────────────────────┴─────────────────────┘
     grid-template-columns: 1fr 350px
```

### Mobile (ширина ≤ 1024px)
```
┌─────────────────────────────────────────────────┐
│          Основная форма                         │
│          (form-card)                            │
│                                                 │
│  - Название                                     │
│  - Категория                                    │
│  - Контент                                      │
│  - Медиа (если есть)                            │
│  - Кнопка (если есть)                           │
│  - Активен                                      │
└─────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────┐
│          Боковая панель                         │
│          (sidebar-card)                         │
│                                                 │
│  📋 Переменные                                  │
│    • {{name}}                                   │
│    • {{email}}                                  │
│                                                 │
│  💡 Подсказки                                   │
│    • Подсказка 1                                │
│    • Подсказка 2                                │
└─────────────────────────────────────────────────┘
     grid-template-columns: 1fr
```

## Стили

### Grid Layout
```scss
.form-layout {
  display: grid;
  grid-template-columns: 1fr 350px;
  gap: 24px;
}

@media (max-width: 1024px) {
  .form-layout {
    grid-template-columns: 1fr;
  }
}
```

### Variable Item (новый дизайн)
```scss
.variable-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  background: #ede9fe;  // Фиолетовый фон
  color: #7c3aed;       // Фиолетовый текст
  border-radius: 8px;
  font-size: 14px;
  font-family: 'Courier New', monospace;
}
```

### Empty State
```scss
.empty-variables {
  text-align: center;
  padding: 24px 16px;
  background: #f9fafb;
  border-radius: 8px;
}

.empty-variables mat-icon {
  font-size: 40px;
  width: 40px;
  height: 40px;
  color: #9ca3af;
  margin-bottom: 8px;
}
```

### Tips List
```scss
.tips-list li::before {
  content: '💡';
  margin-right: 8px;
}
```

## Измененные файлы

### WhatsApp Templates
- ✅ `whatsapp-template-form.component.html` - добавлен sidebar layout
- ✅ `whatsapp-template-form.component.scss` - добавлены стили sidebar
- ✅ `whatsapp-template-form.component.ts` - удален `MatChipsModule`

### Telegram Templates  
- ✅ `telegram-template-form.component.html` - добавлен sidebar layout
- ✅ `telegram-template-form.component.scss` - добавлены стили sidebar
- ✅ `telegram-template-form.component.ts` - удален `MatChipsModule`

### Email Templates
- ✅ `email-template-form.component.html` - добавлен sidebar layout
- ✅ `email-template-form.component.scss` - добавлены стили sidebar
- ✅ `email-template-form.component.ts` - удален `MatChipsModule`

### SMS Templates
- ℹ️ Без изменений (уже использовал sidebar дизайн)

## Удаленные зависимости

Из всех компонентов удален импорт `MatChipsModule`, так как теперь используются кастомные `<div class="variable-item">` вместо `<mat-chip>`.

**Было:**
```typescript
import { MatChipsModule } from '@angular/material/chips';

imports: [
  // ...
  MatChipsModule,
]
```

**Стало:**
```typescript
// MatChipsModule удален

imports: [
  // ...
  // MatChipsModule больше не нужен
]
```

## Преимущества нового дизайна

### 1. Единообразие ✅
Все 4 типа шаблонов (SMS, WhatsApp, Telegram, Email) имеют одинаковый layout

### 2. Лучшая видимость ✅
Переменные всегда видны на экране, не нужно прокручивать вниз

### 3. Меньше зависимостей ✅
Удален `MatChipsModule` из 3 компонентов

### 4. Адаптивность ✅
На мобильных устройствах sidebar перемещается под форму

### 5. Расширяемость ✅
Легко добавить дополнительные секции в sidebar (подсказки, статистика, и т.д.)

### 6. Профессиональный вид ✅
Sidebar дизайн более современный и структурированный

## Подсказки в Sidebar

Каждый тип шаблона имеет свои специфичные подсказки:

### WhatsApp
```html
<li>Используйте переменные для персонализации</li>
<li>Поддерживаются медиафайлы до 16MB</li>
<li>Кнопка действия опциональна</li>
```

### Telegram
```html
<li>Используйте переменные для персонализации</li>
<li>Поддерживаются медиафайлы до 20MB</li>
<li>Inline-кнопки можно настроить позже</li>
```

### Email
```html
<li>Используйте переменные для персонализации</li>
<li>HTML версия обязательна, текстовая - опциональна</li>
<li>Тестируйте шаблоны перед отправкой</li>
```

### SMS
```html
<li>Используйте переменные для персонализации</li>
<li>160 символов = 1 SMS сообщение</li>
<li>Кириллица учитывается как 70 символов</li>
```

## Тестирование

### Что проверить:
1. ✅ Переменные отображаются в sidebar справа
2. ✅ При добавлении переменной в текст, она появляется в списке
3. ✅ При удалении переменной из текста, она исчезает из списка
4. ✅ Empty state показывается когда нет переменных
5. ✅ На мобильных устройствах sidebar под формой
6. ✅ Подсказки отображаются корректно
7. ✅ Все иконки загружаются

## Компиляция

Все компоненты скомпилированы без ошибок:
- ✅ WhatsApp Template Form
- ✅ Telegram Template Form  
- ✅ Email Template Form
- ✅ SMS Template Form (без изменений)
