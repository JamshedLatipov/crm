apiVersion: apps/v1
kind: Deployment
metadata:
  name: deal-service
  namespace: crm
  labels:
    app: deal-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deal-service
  template:
    metadata:
      labels:
        app: deal-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3013"
    spec:
      containers:
        - name: deal-service
          image: ghcr.io/crm/deal-service:latest
          ports:
            - containerPort: 3013
          envFrom:
            - configMapRef:
                name: crm-config
            - secretRef:
                name: crm-secrets
          env:
            - name: PORT
              value: "3013"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3013
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3013
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: deal-service
  namespace: crm
spec:
  type: ClusterIP
  ports:
    - port: 3013
      targetPort: 3013
  selector:
    app: deal-service
