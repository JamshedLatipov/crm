apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaign-service
  namespace: crm
  labels:
    app: campaign-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: campaign-service
  template:
    metadata:
      labels:
        app: campaign-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3018"
    spec:
      containers:
        - name: campaign-service
          image: ghcr.io/crm/campaign-service:latest
          ports:
            - containerPort: 3018
          envFrom:
            - configMapRef:
                name: crm-config
            - secretRef:
                name: crm-secrets
          env:
            - name: PORT
              value: "3018"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3018
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3018
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: campaign-service
  namespace: crm
spec:
  type: ClusterIP
  ports:
    - port: 3018
      targetPort: 3018
  selector:
    app: campaign-service
