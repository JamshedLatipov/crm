apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-service
  namespace: crm
  labels:
    app: task-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: task-service
  template:
    metadata:
      labels:
        app: task-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3014"
    spec:
      containers:
        - name: task-service
          image: ghcr.io/crm/task-service:latest
          ports:
            - containerPort: 3014
          envFrom:
            - configMapRef:
                name: crm-config
            - secretRef:
                name: crm-secrets
          env:
            - name: PORT
              value: "3014"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3014
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3014
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: crm
spec:
  type: ClusterIP
  ports:
    - port: 3014
      targetPort: 3014
  selector:
    app: task-service
