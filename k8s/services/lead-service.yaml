apiVersion: apps/v1
kind: Deployment
metadata:
  name: lead-service
  namespace: crm
  labels:
    app: lead-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lead-service
  template:
    metadata:
      labels:
        app: lead-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3012"
    spec:
      containers:
        - name: lead-service
          image: ghcr.io/crm/lead-service:latest
          ports:
            - containerPort: 3012
          envFrom:
            - configMapRef:
                name: crm-config
            - secretRef:
                name: crm-secrets
          env:
            - name: PORT
              value: "3012"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3012
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3012
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: lead-service
  namespace: crm
spec:
  type: ClusterIP
  ports:
    - port: 3012
      targetPort: 3012
  selector:
    app: lead-service
