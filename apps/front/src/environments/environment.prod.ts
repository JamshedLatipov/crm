export const environment = {
  production: true,
  // API Gateway endpoint (microservices)
  apiBase: '/api',
  // Legacy monolith endpoint (fallback)
  monolithApiBase: '/api-legacy',
  asteriskHost: '192.168.90.121',
  // WebSocket for real-time notifications
  wsUrl: '',
};
