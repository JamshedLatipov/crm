# Быстрый старт: Настройки каналов

## 🚀 Как начать работу

### Шаг 1: Настройте переменные окружения

Скопируйте файл `.env.example` и заполните необходимые значения:

```bash
cp apps/back/src/app/modules/messages/.env.example .env
```

### Шаг 2: Откройте UI настроек

1. Запустите приложение
2. Перейдите: **Сообщения → Настройки каналов**
3. Нажмите кнопку **"Инициализировать из .env"**

Система автоматически импортирует **36 настроек** из `.env` в базу данных.

## 📋 Список всех настроек

### 📱 SMS (8 настроек)

```bash
# Общие
SMS_PROVIDER=smsru              # smsru | smsc | twilio
SMS_SENDER=CRM                  # Имя отправителя

# SMS.RU
SMS_API_KEY=your_api_key

# SMSC.RU
SMSC_LOGIN=your_login
SMSC_PASSWORD=your_password

# Twilio
TWILIO_ACCOUNT_SID=your_sid
TWILIO_AUTH_TOKEN=your_token
TWILIO_PHONE_NUMBER=+1234567890
```

### 📧 Email (6 настроек)

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@example.com
SMTP_FROM_NAME=CRM System
```

**Популярные провайдеры:**
- Gmail: `smtp.gmail.com:587`
- Outlook: `smtp-mail.outlook.com:587`
- SendGrid: `smtp.sendgrid.net:587` (user=`apikey`, pass=`your_api_key`)

### 💬 WhatsApp (5 настроек)

```bash
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_API_KEY=your_api_key
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WHATSAPP_ACCESS_TOKEN=your_access_token
```

**Где получить:**
- [Meta for Developers](https://developers.facebook.com/)
- [WhatsApp Business Platform](https://business.whatsapp.com/)

### 📨 Telegram (4 настройки)

```bash
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_BOT_USERNAME=your_bot_username
TELEGRAM_WEBHOOK_URL=https://your-domain.com/api/telegram/webhook
TELEGRAM_API_URL=https://api.telegram.org
```

**Создание бота:**
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Получите токен

### 🔗 Webhook (6 настроек)

```bash
WEBHOOK_URL=https://your-webhook-endpoint.com/notifications
WEBHOOK_SECRET=your_signing_secret
WEBHOOK_TIMEOUT=5000
WEBHOOK_RETRY_COUNT=3
WEBHOOK_AUTH_TYPE=bearer          # none | basic | bearer | api_key
WEBHOOK_AUTH_VALUE=your_token
```

**Типы авторизации:**
- `none` - без авторизации
- `bearer` - Bearer Token в заголовке
- `basic` - Basic Auth (username:password)
- `api_key` - API Key в заголовке

### ⚙️ Feature Flags (5 настроек)

```bash
FEATURE_SMS_ENABLED=true
FEATURE_EMAIL_ENABLED=true
FEATURE_WHATSAPP_ENABLED=false
FEATURE_TELEGRAM_ENABLED=false
FEATURE_WEBHOOK_ENABLED=true
```

## 🔐 Безопасность

### Секретные значения

Следующие настройки автоматически определяются как **секретные** и шифруются:
- Все, содержащие: `PASSWORD`, `TOKEN`, `API_KEY`, `SECRET`, `AUTH`
- В UI отображаются как `••••••••`
- Шифруются в base64 перед сохранением в БД

### Рекомендации:
1. ✅ Используйте отдельные `.env` для dev/staging/prod
2. ✅ Никогда не коммитьте `.env` в git
3. ✅ Храните production ключи в секретах (K8s Secrets, AWS Secrets Manager, etc.)
4. ✅ Регулярно ротируйте API ключи

## 📝 Управление через UI

### Создание новой настройки

1. Выберите вкладку категории (SMS, Email, etc.)
2. Нажмите кнопку **"Добавить"**
3. Заполните форму:
   - **Ключ**: уникальное имя (например: `SMS_NEW_PROVIDER_KEY`)
   - **Значение**: значение настройки
   - **Категория**: выберите из списка
   - **Описание**: краткое описание (опционально)
   - **Секретное значение**: ✓ если это пароль/токен
   - **Шифровать**: ✓ для шифрования в БД
4. Нажмите **"Создать"**

### Редактирование

1. Найдите настройку в списке
2. Нажмите иконку **"Редактировать"** (карандаш)
3. Измените нужные поля
4. Нажмите **"Сохранить"**

### Удаление

1. Найдите настройку в списке
2. Нажмите иконку **"Удалить"** (корзина)
3. Подтвердите удаление

## 🧪 Тестирование каналов

> ⚠️ **В разработке**
> 
> Функционал тестирования будет добавлен в следующей версии.
> 
> Планируется:
> - Кнопка "Тест" для каждого канала
> - Отправка пробного сообщения
> - Проверка подключения
> - Отображение результата (успех/ошибка)

## 🔄 Синхронизация с .env

### Как это работает:

1. **Первый запуск**: Нажмите "Инициализировать из .env"
   - Импортируются все значения из `.env`
   - Создаются записи в БД
   - Дубликаты игнорируются (по ключу)

2. **Приоритет значений**: БД > .env
   - Провайдеры сначала читают из БД
   - Если в БД нет - используется `.env`
   - Можно переопределять настройки без изменения кода

3. **Обновление .env**: 
   - Изменения в `.env` НЕ применяются автоматически
   - Нужно снова нажать "Инициализировать из .env"
   - Существующие настройки НЕ перезаписываются

## 📊 Статистика

После инициализации вы получите:
- ✅ **36 настроек** в базе данных
- 🔐 **16 секретных** значений (зашифрованы)
- 📁 **6 категорий**: SMS, Email, WhatsApp, Telegram, Webhook, Feature Flags

## 🆘 Частые проблемы

### "Настройки для этой категории отсутствуют"

**Решение**: Нажмите "Инициализировать из .env" для импорта настроек

### "WhatsApp/Telegram настройки не появились"

**Проверьте**:
1. Есть ли переменные в `.env`?
2. Перезапустили ли backend после изменения `.env`?
3. Нажали ли "Инициализировать из .env"?

### "Не работает отправка"

**Проверьте**:
1. Feature Flag включен? (`FEATURE_SMS_ENABLED=true`)
2. API ключи правильные?
3. Логи backend: `docker logs crm-back`

## 📚 Дополнительная документация

- [Полная документация](./SETTINGS_CHANNEL_DOCUMENTATION.md)
- [API Reference](./SETTINGS_CHANNEL_DOCUMENTATION.md#api-examples)
- [Структура БД](./SETTINGS_CHANNEL_DOCUMENTATION.md#база-данных)

---

**Статус**: ✅ MVP готов  
**Дата**: 06.01.2026  
**Версия**: 1.0.0
